spring:
  profiles:
    include:
      - secret

  jpa:
    properties:
      hibernate:
        use_sql_comments: true
        format_sql: true

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace